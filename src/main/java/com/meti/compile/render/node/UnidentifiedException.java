package com.meti.compile.render.node;

import com.meti.compile.render.CompileException;

public class UnidentifiedException extends CompileException {
    public UnidentifiedException(String message) {
        super(message);
    }
}
