package com.meti.compile.render.node;

import com.meti.compile.render.CompileException;

public class UnvaluedException extends CompileException {
    public UnvaluedException(String message) {
        super(message);
    }
}
